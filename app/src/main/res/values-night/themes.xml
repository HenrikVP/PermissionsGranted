<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PermissionsGranted" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="Theme.MyCustomTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary branding color. -->
        <item name="colorPrimary">@color/primary_orange</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/text_color</item>

        <!-- Secondary branding color. -->
        <item name="colorSecondary">@color/primary_yellow</item>
        <item name="colorSecondaryVariant">@color/primary_light</item>
        <item name="colorOnSecondary">@color/text_color</item>

        <!-- Background color. -->
        <item name="android:colorBackground">@color/background_dark_grey_night</item>

        <!-- Surface color -->
        <item name="colorSurface">@color/background_dark_grey</item>
        <item name="colorOnSurface">@color/text_color</item>

        <!-- Error color -->
        <item name="colorError">#FF5252</item>
        <item name="colorOnError">#FFFFFF</item>
    </style>
</resources>